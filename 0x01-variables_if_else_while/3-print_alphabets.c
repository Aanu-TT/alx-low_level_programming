#include <stdio.h>
/**
 * main - program that runs both upper and lower case alphabets using putchar function
 * Return: 0 (Success)
 */
int main(void)
{
	char(letter)

	for(letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);

	for(letter = 'A'; letter <= 'Z'; letter++)
		putchar(letter);

	putchar('\n');

	return (0);
}
